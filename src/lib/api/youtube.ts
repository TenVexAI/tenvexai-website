import { YouTubeShort } from '@/types';

/**
 * Fetch YouTube shorts from channel
 * TODO: Implement with real YouTube Data API v3
 * 
 * @param limit - Number of shorts to fetch
 * @returns Mock shorts data for now
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function getYouTubeShorts(limit = 6): Promise<YouTubeShort[]> {
  // Mock data - replace with actual YouTube API call
  // Return empty array for now since there's no content yet
  return [];
}
